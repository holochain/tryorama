[package]
edition = "2021"
name = "coordinator"
version = "0.1.0"

[lib]
crate-type = ["cdylib", "rlib"]
name = "coordinator"

[dependencies]
hdk = "0.0.163"
integrity = {path = "../integrity"}
serde = "1"
